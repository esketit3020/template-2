# Public site URL — used for canonicals, sitemap and JSON-LD absolute URLs.
NEXT_PUBLIC_SITE_URL="https://www.ironbarkroofing.com.au"

# --- Lead delivery (build checklist §4: "Form submissions routed to correct inbox/CRM") ---
# Not yet wired. /api/quote validates + logs until these are supplied by the client.
QUOTE_INBOX="quotes@ironbarkroofing.com.au"
RESEND_API_KEY=""

# --- Tracking (build checklist §5) ---
# Leave blank to disable. GA4 script is only injected when this is set.
NEXT_PUBLIC_GA4_ID=""
NEXT_PUBLIC_GTM_ID=""

# --- Spam protection (build checklist §4) ---
# Honeypot + timing check are always on. reCAPTCHA is added when keys are present.
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=""
RECAPTCHA_SECRET_KEY=""

# --- Phase 2: live Google reviews (design doc open item 3) ---
GOOGLE_PLACES_API_KEY=""
GOOGLE_PLACE_ID=""
